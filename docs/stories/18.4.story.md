# Story 18.4: Testes — Cenários de Upgrade/Downgrade

## Status

**Draft**

## Executor Assignment

```yaml
executor: '@dev'
quality_gate: '@qa'
quality_gate_tools: ['npm run lint', 'npm run typecheck', 'npm run build', 'npm test']
```

## Story

**As a** desenvolvedor da plataforma,
**I want** testes unitários cobrindo os cenários de troca de plano (upgrade, downgrade, mesmo plano, sem assinatura),
**so that** regressões no fluxo crítico de billing sejam detectadas antes de chegarem à produção.

## Business Value

O fluxo de troca de plano envolve chamadas ao Stripe com impacto financeiro direto. Um bug nesse fluxo pode resultar em cobranças incorretas, proration errada ou acesso indevido a conteúdo. Testes automatizados com mock do Stripe garantem que a lógica de validação e integração seja verificada a cada `npm test` — sem depender de ambiente Stripe real.

## Complexity Estimate

**T-shirt:** S (Small) — ~2-3h
**Story Points:** 2

## Scope

### IN

Testes unitários para `src/app/api/plan/change/route.ts`:

- Cenário 1: **Upgrade bem-sucedido** — plano mensal → plano anual ou plano inferior → superior; espera `200` com `{ success: true, prorationAmount, nextBillingDate }`
- Cenário 2: **Downgrade bem-sucedido** — plano superior → inferior; mesma estrutura de resposta
- Cenário 3: **Mesmo plano** — request com `newPlanId` e `billingPeriod` iguais ao plano atual; espera `400` com `{ error: 'Already on this plan' }`
- Cenário 4: **Sem assinatura ativa** — usuário sem subscription no banco; espera `400` com `{ error: 'No active subscription found' }`
- Cenário 5: **Sem autenticação** — request sem session válida; espera `401`

### OUT

- Testes de integração com Stripe real (usar apenas mocks)
- Testes E2E de UI (cobertos manualmente ou em stories de QA futuras)
- Testes do webhook 18.3 (podem ser adicionados como extensão, mas não são obrigatórios para esta story)

## Dependencies

- **Story 18.1 deve estar completa** — código do endpoint deve existir para ser testado
- Framework de testes: Vitest (verificar `vitest.config.mts` — já presente no projeto)
- Padrão de testes existente em `tests/` (verificar estrutura atual)

## Acceptance Criteria

- **AC-1:** Arquivo de teste criado em `tests/api/plan/change.test.ts` (ou caminho compatível com `vitest.config.mts`)
- **AC-2:** Teste de upgrade retorna `200` com `{ success: true }` e `prorationAmount` numérico
- **AC-3:** Teste de downgrade retorna `200` com `{ success: true }` e `prorationAmount` numérico (pode ser negativo)
- **AC-4:** Teste de mesmo plano retorna `400` com `{ error: 'Already on this plan' }`
- **AC-5:** Teste de sem assinatura ativa retorna `400` com `{ error: 'No active subscription found' }`
- **AC-6:** Teste de sem autenticação retorna `401`
- **AC-7:** `npm test` passa com todos os cenários verdes (0 failures)

## Technical Notes

### Estrutura de teste sugerida

```typescript
// tests/api/plan/change.test.ts
import { describe, it, expect, vi, beforeEach } from 'vitest'

// Mock Supabase
vi.mock('@supabase/auth-helpers-nextjs', () => ({
  createRouteHandlerClient: vi.fn(),
}))

// Mock Stripe
vi.mock('stripe', () => {
  return {
    default: vi.fn().mockImplementation(() => ({
      subscriptions: {
        retrieve: vi.fn(),
        update: vi.fn(),
      },
      invoices: {
        retrieveUpcoming: vi.fn(),
      },
    })),
  }
})

describe('POST /api/plan/change', () => {
  it('retorna 401 sem autenticação', async () => { ... })
  it('retorna 400 sem subscription ativa', async () => { ... })
  it('retorna 400 para mesmo plano', async () => { ... })
  it('retorna 200 para upgrade bem-sucedido', async () => { ... })
  it('retorna 200 para downgrade bem-sucedido', async () => { ... })
})
```

### Padrão de mock do Supabase

Verificar como os outros testes em `tests/` mockam o Supabase client antes de implementar — seguir o padrão existente no projeto.

### Verificar framework

O arquivo `vitest.config.mts` está presente na raiz. Verificar configuração de alias e setup para Next.js App Router (pode precisar de `@vitejs/plugin-react` ou similar).

### Mock de dados de teste

```typescript
const mockSubscription = {
  stripe_subscription_id: 'sub_test123',
  plan_id: 'prata',
  billing_period: 'monthly',
  status: 'active',
}

const mockPlan = {
  id: 'ouro',
  stripe_price_id_monthly: 'price_ouro_monthly',
  stripe_price_id_annual: 'price_ouro_annual',
}

const mockStripeSubscription = {
  items: { data: [{ id: 'si_test123', price: { id: 'price_prata_monthly' } }] },
  customer: 'cus_test123',
  current_period_end: Math.floor(Date.now() / 1000) + 86400 * 30,
}

const mockUpcomingInvoice = {
  amount_due: 2500, // R$ 25,00 em centavos
}
```

## Tasks / Subtasks

- [ ] **Task 1 — Setup do arquivo de teste** (AC-1)
  - Criar `tests/api/plan/change.test.ts`
  - Configurar mocks de Supabase e Stripe
  - Verificar compatibilidade com `vitest.config.mts`

- [ ] **Task 2 — Implementar cenários de erro** (AC-4, AC-5, AC-6)
  - Teste 401: sem session
  - Teste 400: sem subscription ativa
  - Teste 400: mesmo plano

- [ ] **Task 3 — Implementar cenários de sucesso** (AC-2, AC-3)
  - Teste 200: upgrade (prata mensal → ouro mensal)
  - Teste 200: downgrade (diamante → prata)
  - Verificar estrutura do response: `{ success, prorationAmount, nextBillingDate }`

- [ ] **Task 4 — Rodar e verificar** (AC-7)
  - `npm test` — todos os cenários passam
  - `npm run lint` sem erros
  - `npm run typecheck` sem erros
  - `npm run build` sem erros

## File List

| File                            | Action | Notes                                              |
| ------------------------------- | ------ | -------------------------------------------------- |
| `tests/api/plan/change.test.ts` | Create | Testes unitários do endpoint POST /api/plan/change |

## CodeRabbit Integration

> **Story Type:** Testing
> **Primary Type:** Testing
> **Complexity:** Low

**Self-Healing Configuration:**

- Primary Agent: @dev (YOLO mode)
- Max Iterations: 2
- Severity Filter: CRITICAL only

**Focus Areas:**

- Todos os cenários do escopo cobertos (sem cenário faltante)
- Mocks completos: Supabase client + Stripe SDK
- Asserções explícitas em status HTTP e corpo da resposta
- Isolamento de testes: cada `it` limpa estado dos mocks (beforeEach/afterEach)

## Change Log

| Date       | Agent       | Action                                                                   |
| ---------- | ----------- | ------------------------------------------------------------------------ |
| 2026-06-17 | @sm (River) | Story 18.4 criada — Testes Cenários de Upgrade/Downgrade — Status: Draft |
