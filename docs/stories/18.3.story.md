# Story 18.3: Webhook — Handler para customer.subscription.updated

## Status

**Draft**

## Executor Assignment

```yaml
executor: '@dev'
quality_gate: '@qa'
quality_gate_tools: ['npm run lint', 'npm run typecheck', 'npm run build']
```

## Story

**As a** plataforma EAD (sistema),
**I want** que o webhook do Stripe processe o evento `customer.subscription.updated` e atualize os dados de plano do usuário no banco,
**so that** após uma troca de plano via Story 18.1, o banco reflita o novo plano sem intervenção manual.

## Business Value

O webhook é o elo de sincronização entre Stripe e o banco de dados da plataforma. Sem ele, a subscription é atualizada no Stripe mas o usuário continua aparecendo com o plano antigo na plataforma. O handler de `customer.subscription.updated` é idempotente por design — processar o mesmo evento duas vezes não corrompe dados, garantindo resiliência em caso de retries do Stripe.

## Complexity Estimate

**T-shirt:** S (Small) — ~2-3h
**Story Points:** 2

## Scope

### IN

- Adicionar handler para o evento `customer.subscription.updated` no arquivo de webhook existente (`src/app/api/webhooks/stripe/route.ts`)
- Extrair do evento: `customer` (Stripe customer ID), `status`, `items.data[0].price.id`
- Mapear `price_id` para `plan_id` consultando a tabela `plans` no banco
- Atualizar tabela `subscriptions`: `plan_id`, `billing_period`, `status`, `current_period_end`
- Atualizar tabela `users`: `subscription_plan` (campo existente do Epic 16)
- Garantir idempotência: se o evento já foi processado (mesmos dados), não duplicar atualizações

### OUT

- Criação de nova enrollment/matrícula em cursos (não é responsabilidade deste evento)
- Lógica de cancelamento de subscription (`customer.subscription.deleted` — Epic separado se necessário)
- Processamento de pagamento (`payment_intent.succeeded` — já tratado no Epic 16)
- Rollback de acesso a cursos em caso de downgrade (lógica de acesso já é baseada no `plan_id` atual)

## Dependencies

- **Story 18.1 deve estar completa** — sem a API de troca, este webhook não é acionado
- Webhook existente em `src/app/api/webhooks/stripe/route.ts` do Epic 16
- Tabela `plans` com campo `stripe_price_id_monthly` e `stripe_price_id_annual`
- Tabela `subscriptions` com campos: `user_id`, `plan_id`, `billing_period`, `status`, `current_period_end`, `stripe_subscription_id`
- Tabela `users` com campo `subscription_plan`
- Variável `STRIPE_WEBHOOK_SECRET` (já presente)

## Acceptance Criteria

- **AC-1:** O evento `customer.subscription.updated` é reconhecido pelo switch/router de eventos no webhook e direcionado para o handler correto
- **AC-2:** Após processar o evento, a tabela `subscriptions` reflete o novo `plan_id`, `billing_period` e `current_period_end` do usuário
- **AC-3:** Após processar o evento, `users.subscription_plan` é atualizado para o novo plano
- **AC-4:** O handler é idempotente: processar o mesmo evento Stripe duas vezes não gera duplicatas nem inconsistências (verificável via `event.id` ou comparação de dados atuais)
- **AC-5:** Um evento com `status` diferente de `'active'` (ex: `'past_due'`, `'canceled'`) atualiza apenas o `status` na tabela `subscriptions`, sem alterar `plan_id`
- **AC-6:** O webhook retorna `200 OK` após processamento bem-sucedido (comportamento já esperado pelo Stripe para evitar retries)
- **AC-7:** Log de erro é registrado (`console.error`) se ocorrer falha ao buscar o `plan_id` pelo `price_id` (price_id desconhecido)

## Technical Notes

### Arquivo a modificar

`src/app/api/webhooks/stripe/route.ts`

### Handler a adicionar

```typescript
case 'customer.subscription.updated': {
  const subscription = event.data.object as Stripe.Subscription
  const customerId = subscription.customer as string
  const priceId = subscription.items.data[0].price.id
  const newStatus = subscription.status
  const currentPeriodEnd = new Date(subscription.current_period_end * 1000).toISOString()

  if (newStatus !== 'active') {
    // Atualizar apenas status
    await supabase
      .from('subscriptions')
      .update({ status: newStatus })
      .eq('stripe_subscription_id', subscription.id)
    break
  }

  // Buscar plan_id pelo price_id
  const { data: plan } = await supabase
    .from('plans')
    .select('id, billing_period')
    .or(`stripe_price_id_monthly.eq.${priceId},stripe_price_id_annual.eq.${priceId}`)
    .single()

  if (!plan) {
    console.error(`[webhook] price_id desconhecido: ${priceId}`)
    break
  }

  const billingPeriod = plan.stripe_price_id_annual === priceId ? 'annual' : 'monthly'

  // Atualizar subscriptions
  await supabase
    .from('subscriptions')
    .update({
      plan_id: plan.id,
      billing_period: billingPeriod,
      status: newStatus,
      current_period_end: currentPeriodEnd,
    })
    .eq('stripe_subscription_id', subscription.id)

  // Atualizar users.subscription_plan
  // Buscar user_id via stripe_customer_id
  const { data: userSub } = await supabase
    .from('subscriptions')
    .select('user_id')
    .eq('stripe_subscription_id', subscription.id)
    .single()

  if (userSub) {
    await supabase
      .from('users')
      .update({ subscription_plan: plan.id })
      .eq('id', userSub.user_id)
  }
  break
}
```

### Idempotência

O handler usa `.update()` com `.eq('stripe_subscription_id', subscription.id)` — atualizar os mesmos valores duas vezes não causa duplicatas. O Stripe garante entrega at-least-once, portanto idempotência é obrigatória.

### Padrão existente no webhook

O arquivo `src/app/api/webhooks/stripe/route.ts` já possui a estrutura de verificação de assinatura e switch de eventos do Epic 16. Adicionar o novo `case` seguindo o padrão existente.

## Tasks / Subtasks

- [ ] **Task 1 — Adicionar case no switch de eventos** (AC-1)
  - Localizar switch/router de eventos em `src/app/api/webhooks/stripe/route.ts`
  - Adicionar `case 'customer.subscription.updated'`

- [ ] **Task 2 — Implementar lógica de atualização** (AC-2, AC-3, AC-5, AC-7)
  - Extrair `priceId`, `status`, `current_period_end` do evento
  - Se `status !== 'active'`: atualizar apenas `status` na tabela `subscriptions`
  - Se `status === 'active'`: buscar `plan_id` pelo `price_id` na tabela `plans`
  - Atualizar `subscriptions` (plan_id, billing_period, status, current_period_end)
  - Atualizar `users.subscription_plan`
  - Log de erro para `price_id` desconhecido

- [ ] **Task 3 — Verificar idempotência** (AC-4)
  - Testar processamento duplo do mesmo evento não gera inconsistências

- [ ] **Task 4 — QA e build**
  - `npm run lint` sem erros
  - `npm run typecheck` sem erros
  - `npm run build` sem erros

## File List

| File                                   | Action | Notes                                                |
| -------------------------------------- | ------ | ---------------------------------------------------- |
| `src/app/api/webhooks/stripe/route.ts` | Modify | Adicionar handler para customer.subscription.updated |

## CodeRabbit Integration

> **Story Type:** API + Integration (Stripe Webhook)
> **Primary Type:** API
> **Complexity:** Low-Medium

**Self-Healing Configuration:**

- Primary Agent: @dev (YOLO mode)
- Max Iterations: 2
- Severity Filter: CRITICAL, HIGH

**Focus Areas:**

- Idempotência obrigatória: `.update()` nunca `.insert()` em handler de subscription.updated
- Verificação de assinatura Stripe (já existente no arquivo — não remover)
- `console.error` para price_id desconhecido (não fazer throw — evitar retry infinito do Stripe)
- Retorno `200 OK` mesmo em erros de lógica de negócio (para evitar retries do Stripe)

## Change Log

| Date       | Agent       | Action                                                                            |
| ---------- | ----------- | --------------------------------------------------------------------------------- |
| 2026-06-17 | @sm (River) | Story 18.3 criada — Webhook Handler customer.subscription.updated — Status: Draft |
