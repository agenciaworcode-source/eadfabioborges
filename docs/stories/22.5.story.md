# Story 22.5: Quiz Avançado — 5 Novos Tipos + Timer + Feedback Mode + Randomização

## Status

**Ready**

## Executor Assignment

```yaml
executor: '@dev'
quality_gate: '@qa'
quality_gate_tools: ['npm run lint', 'npm run typecheck', 'npm run build']
```

## Story

**As a** Fábio Borges (administrador da plataforma),
**I want** um quiz avançado com 5 tipos de pergunta, timer, modos de feedback e randomização de questões,
**so that** eu possa criar avaliações desafiadoras e variadas como as do Tutor LMS.

## Business Value

O quiz atual tem apenas 3 tipos (múltipla escolha, V/F, dissertativa) e zero configuração avançada. O Tutor LMS oferece 8 tipos de questão e configurações de timer, feedback e randomização que tornam os quizzes muito mais eficazes pedagogicamente. Para um curso de estética, questões de preenchimento de lacunas, correspondência e ordenação são fundamentais para testar conhecimento de procedimentos e sequências técnicas.

## Complexity Estimate

**T-shirt:** L (Large) — ~6h
**Story Points:** 8

## Scope

### IN

**Configurações do quiz (QuizBuilder.tsx — seção de settings):**

- Timer: `time_limit_secs` — input número em minutos (converter para segundos ao salvar; 0 = sem limite)
- Feedback mode: `feedback_mode` — select com 3 opções:
  - `after_all` (padrão) — "Exibir gabarito após concluir o quiz"
  - `after_each` — "Exibir resposta após cada questão"
  - `unlimited_retry` — "Aluno tenta até acertar (sem limite)"
- Randomizar questões: `randomize_questions` — checkbox
- Limite de questões por tentativa: `max_questions_shown` — input número (0 = todas)

**5 novos tipos de questão no QuestionForm:**

| Tipo             | Descrição         | Como funciona                                                                                                         |
| ---------------- | ----------------- | --------------------------------------------------------------------------------------------------------------------- |
| `fill_in_blanks` | Preencher lacuna  | Texto com `___` substituído por input; `correct_answer` = "resposta correta"                                          |
| `short_answer`   | Resposta curta    | Textarea limitado; correção manual pelo admin (sem gabarito automático)                                               |
| `matching`       | Correspondência   | Duas colunas (pares); `options` = `[{left:"A",right:"1"}, ...]`; aluno arrasta ou seleciona                           |
| `ordering`       | Ordenação         | Lista de itens em ordem errada; aluno arrasta para a ordem certa; `options` = itens; `correct_answer` = order correto |
| `image_choice`   | Escolha de imagem | Como múltipla escolha mas opções são URLs de imagens; `options` = URLs                                                |

**QuizBuilder.tsx:**

- Adicionar seção "Configurações do Quiz" (colapsável) com os 4 novos campos
- Atualizar PATCH/POST de quiz para incluir novos campos
- Atualizar QuestionForm para suportar os 5 novos tipos no select de tipo

**Renderização no player (área do aluno):**

- `fill_in_blanks`: parsear o texto e substituir `___` por `<input>` inline
- `short_answer`: `<textarea>` simples
- `matching`: lista de pares com select ou drag (UI simples: select dropdown por item)
- `ordering`: lista arrastável (dnd-kit) ou botões ↑↓ por item
- `image_choice`: grid de imagens clicáveis

**Correção automática:**

- `fill_in_blanks`: comparar (case-insensitive, trim) com `correct_answer`
- `multiple_choice`, `true_false`, `image_choice`, `ordering`: comparar com `correct_answer`
- `short_answer`, `matching`: marcar como "pendente de correção manual" (score = null, sem auto-pass)

**Timer no player:**

- Se `time_limit_secs > 0`: exibir countdown regressivo ao iniciar tentativa de quiz
- Ao zerar o tempo: submeter quiz automaticamente com as respostas até o momento

**Feedback mode no player:**

- `after_all`: exibir resultado apenas na tela de conclusão do quiz
- `after_each`: ao responder cada questão, exibir se acertou/errou antes de ir para próxima
- `unlimited_retry`: ao errar, exibir feedback e permitir tentar novamente sem contar como nova tentativa

### OUT

- Pool de questões com banco de questões reutilizáveis entre quizzes (requer nova tabela)
- Peso diferente por questão (score ponderado)
- Importação de questões via CSV
- Preview do quiz como aluno direto do admin

## Dependencies

- **Story 22.1** deve estar concluída (`time_limit_secs`, `feedback_mode`, `randomize_questions`, `max_questions_shown` em `quizzes`; `question_order` em `questions`)
- **Story 22.3** deve estar concluída se `ordering` usar dnd-kit (compartilhar a instalação do pacote)
- Componentes existentes: `QuizBuilder.tsx`, `QuestionForm.tsx` (inferido do mapeamento)
- Player de quiz existente — localizar e ler antes de implementar

## Acceptance Criteria

- **AC-1:** QuizBuilder exibe seção "Configurações" com campos timer (minutos), feedback mode (select), randomizar (checkbox) e limite de questões (número)
- **AC-2:** Ao salvar o quiz, as 4 configurações são persistidas no banco
- **AC-3:** O select de tipo de questão oferece os 8 tipos: multiple_choice, true_false, essay (dissertativa), fill_in_blanks, short_answer, matching, ordering, image_choice
- **AC-4:** QuestionForm renderiza UI correta para cada novo tipo (fill_in_blanks, short_answer, matching, ordering, image_choice)
- **AC-5:** No player do aluno, fill_in_blanks exibe campo input inline no texto
- **AC-6:** No player do aluno, matching exibe duas colunas para correspondência
- **AC-7:** No player do aluno, ordering exibe lista com controles de reordenação
- **AC-8:** Se `time_limit_secs > 0`, o timer regressivo é exibido durante a tentativa
- **AC-9:** O feedback mode `after_each` exibe resultado após cada questão
- **AC-10:** Tipos existentes (multiple_choice, true_false, essay) continuam funcionando sem regressão
- **AC-11:** Lint, typecheck e build passam sem erros

## Technical Notes

### Modelo de dados para novos tipos

```typescript
// questions.options (jsonb) — uso por tipo:

// fill_in_blanks:
// body: "A técnica de ___ é indicada para peles sensíveis"
// correct_answer: "hidratação"
// options: null

// short_answer:
// body: "Descreva o processo de limpeza de pele profunda"
// correct_answer: null (correção manual)
// options: null

// matching:
// body: "Relacione o procedimento com a sua indicação"
// options: [{"left": "Peeling", "right": "Renovação celular"}, {"left": "Drenagem", "right": "Edema"}]
// correct_answer: "Peeling:Renovação celular|Drenagem:Edema" (ou JSON string)

// ordering:
// body: "Ordene as etapas da limpeza de pele"
// options: ["Hidratação", "Extração", "Tonificação", "Limpeza"] // ordem embaralhada para o aluno
// correct_answer: "Limpeza|Extração|Tonificação|Hidratação" // ordem correta

// image_choice:
// body: "Identifique o tipo de pele na imagem"
// options: ["https://url1.jpg", "https://url2.jpg", "https://url3.jpg"]
// correct_answer: "https://url1.jpg"
```

### QuestionForm — renderização por tipo

```tsx
switch (question.type) {
  case 'fill_in_blanks':
    return <FillInBlanksForm question={question} onChange={onChange} />
  case 'short_answer':
    return <ShortAnswerForm question={question} onChange={onChange} />
  case 'matching':
    return <MatchingForm question={question} onChange={onChange} />
  case 'ordering':
    return <OrderingForm question={question} onChange={onChange} />
  case 'image_choice':
    return <ImageChoiceForm question={question} onChange={onChange} />
  // tipos existentes...
}
```

### Timer no player

```tsx
const [timeLeft, setTimeLeft] = useState(quiz.time_limit_secs)
useEffect(() => {
  if (!quiz.time_limit_secs) return
  const interval = setInterval(() => {
    setTimeLeft((t) => {
      if (t <= 1) {
        clearInterval(interval)
        submitQuiz()
        return 0
      }
      return t - 1
    })
  }, 1000)
  return () => clearInterval(interval)
}, [])
// Exibir: Math.floor(timeLeft/60) + ':' + String(timeLeft%60).padStart(2,'0')
```

### Randomização de questões

```typescript
// Ao iniciar tentativa de quiz:
let questionsToShow = [...questions]
if (quiz.randomize_questions) {
  questionsToShow = questionsToShow.sort(() => Math.random() - 0.5)
}
if (quiz.max_questions_shown > 0) {
  questionsToShow = questionsToShow.slice(0, quiz.max_questions_shown)
}
```

### API route de quiz

Verificar endpoint existente `PATCH /api/admin/quizzes/[id]` ou similar. Adicionar os 4 novos campos ao handler.

### Correção automática de fill_in_blanks

```typescript
function checkFillInBlanks(userAnswer: string, correctAnswer: string): boolean {
  return userAnswer.trim().toLowerCase() === correctAnswer.trim().toLowerCase()
}
```

## Tasks / Subtasks

- [ ] **Task 1 — Seção de Configurações no QuizBuilder** (AC-1, AC-2)
  - Adicionar UI de timer (minutos), feedback_mode (select), randomize_questions (checkbox), max_questions_shown (número)
  - Atualizar PATCH/POST do quiz para incluir novos campos

- [ ] **Task 2 — Novos tipos no select de QuestionForm** (AC-3)
  - Adicionar os 5 novos tipos ao select de tipo de questão
  - Garantir tipos existentes ainda presentes

- [ ] **Task 3 — Forms de criação dos novos tipos** (AC-4)
  - `FillInBlanksForm`: campo body com instrução de usar `___`, campo correct_answer
  - `ShortAnswerForm`: campo body, sem correct_answer (label: "Correção manual")
  - `MatchingForm`: editor de pares esquerda↔direita (adicionar/remover pares)
  - `OrderingForm`: editor de lista de itens + campo de ordem correta
  - `ImageChoiceForm`: campos de URL para cada opção + seleção da correta

- [ ] **Task 4 — Renderização no player (área do aluno)** (AC-5, AC-6, AC-7)
  - `fill_in_blanks`: parsear body e render input inline
  - `matching`: render duas colunas com selects
  - `ordering`: render lista com botões ↑↓ ou dnd-kit
  - `image_choice`: render grid de imagens clicáveis
  - `short_answer`: render textarea

- [ ] **Task 5 — Timer e feedback mode no player** (AC-8, AC-9)
  - Timer countdown com auto-submit
  - Lógica de feedback_mode: after_each mostra resultado por questão
  - Randomização e limite de questões ao iniciar tentativa

- [ ] **Task 6 — QA e build** (AC-10, AC-11)
  - `npm run lint` sem erros
  - `npm run typecheck` sem erros
  - `npm run build` sem erros
  - Testar: quiz existente não regrediu, todos os novos tipos funcionam, timer funciona

## File List

| File                                      | Action | Notes                                               |
| ----------------------------------------- | ------ | --------------------------------------------------- |
| `src/components/admin/QuizBuilder.tsx`    | Update | Seção de configurações + novos tipos no select      |
| `src/components/admin/QuestionForm.tsx`   | Update | Forms dos 5 novos tipos de questão                  |
| `src/components/quiz/QuizPlayer.tsx`      | Update | Renderização de novos tipos + timer + feedback mode |
| `src/app/api/admin/quizzes/[id]/route.ts` | Update | Aceitar 4 novos campos de configuração              |

## CodeRabbit Integration

> **Story Type:** Frontend + API
> **Primary Type:** Frontend
> **Complexity:** High (5 novos tipos de questão, timer, lógica de feedback, randomização)

**Specialized Agent Assignment:**

- Primary: @dev

**Quality Gate Tasks:**

- [ ] Pre-Commit (@dev): Run `coderabbit --prompt-only -t uncommitted`
- [ ] Pre-PR (@devops): Run `coderabbit --prompt-only --base main`

**Focus Areas:**

- Regressão nos 3 tipos de questão existentes (multiple_choice, true_false, essay)
- Memory leak no interval do timer (clearInterval no unmount obrigatório)
- XSS: `image_choice` URLs devem ser validadas (não usar innerHTML)
- Matching/ordering: serialização e deserialização do jsonb deve ser consistente

**Self-Healing:**

- Mode: light | Max Iterations: 2 | Severity: CRITICAL

## Change Log

| Date       | Agent       | Action                                                                           |
| ---------- | ----------- | -------------------------------------------------------------------------------- |
| 2026-06-19 | @sm (River) | Story 22.5 criada — Quiz Avançado 5 Tipos + Timer + Randomização — Status: Draft |
| 2026-06-19 | @po (Pax)   | Validação 10/10 — GO — Status: Draft → Ready                                     |
