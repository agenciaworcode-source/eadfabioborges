# Story 22.3: Curriculum Builder — Drag-and-Drop + Rich Text + Sumário de Módulo

## Status

**Ready**

## Executor Assignment

```yaml
executor: '@dev'
quality_gate: '@qa'
quality_gate_tools: ['npm run lint', 'npm run typecheck', 'npm run build']
```

## Story

**As a** Fábio Borges (administrador da plataforma),
**I want** que eu possa reordenar módulos e aulas arrastando-os, adicionar uma descrição ao módulo e usar um editor de texto rico nas aulas de texto,
**so that** a criação de conteúdo seja fluida e profissional como no Tutor LMS.

## Business Value

Os botões ↑↓ são lentos e frustrantes ao reordenar um currículo com 10+ módulos e 50+ aulas. O drag-and-drop é o padrão de mercado em todo LMS profissional. Adicionalmente, o textarea puro para aulas de texto impede qualquer formatação — negrito, listas, links, imagens inline — que são essenciais para criar aulas de texto de qualidade.

## Complexity Estimate

**T-shirt:** L (Large) — ~6h
**Story Points:** 8

## Scope

### IN

**Drag-and-drop de módulos:**

- Instalar `@dnd-kit/core` e `@dnd-kit/sortable` (se não presente em package.json)
- Substituir botões ↑↓ em `ModuleEditor.tsx` por handles de drag-and-drop
- Ao soltar, fazer PATCH nos campos `order` dos módulos afetados

**Drag-and-drop de aulas:**

- Substituir botões ↑↓ em `LessonEditor.tsx` por handles de drag-and-drop
- Manter drag-and-drop DENTRO do mesmo módulo apenas (reordenação intra-módulo)
- Ao soltar, fazer PATCH nos campos `order` das aulas afetadas

**Campo `description` no módulo:**

- Adicionar campo textarea (2 linhas, resize vertical) "Descrição/sumário do módulo" no formulário de criação/edição do módulo em `ModuleEditor.tsx`
- Incluir no PATCH de atualização do módulo
- Exibir abaixo do título do módulo (colapsado, expandível ou sempre visível — escolher o mais simples)

**Rich text editor para aulas de texto:**

- Instalar `@tiptap/react`, `@tiptap/starter-kit` (se não presente)
- Substituir `<textarea>` de `content_body` no `LessonEditor.tsx` por componente TipTap
- Toolbar básica: negrito, itálico, lista não-ordenada, lista ordenada, link, cabeçalho H2/H3
- Salvar como HTML (string) no campo `content_body` — compatível com o player que já renderiza HTML

### OUT

- Drag-and-drop ENTRE módulos diferentes (mover aula de um módulo para outro) — complexidade excessiva
- Upload de imagens inline no rich text editor (bucket de storage necessário)
- Ordenação de questões de quiz por drag-and-drop (fica na Story 22.5)
- Preview do rich text em tempo real na tela do admin

## Dependencies

- **Story 22.1** deve estar concluída (campo `description` em `modules`)
- **Story 22.2** não é pré-requisito, mas pode ser desenvolvida em paralelo
- Componentes existentes: `ModuleEditor.tsx`, `LessonEditor.tsx`
- Verificar se `@dnd-kit/core` e `@tiptap/react` já estão em `package.json`

## Acceptance Criteria

- **AC-1:** Na aba "Currículo", módulos podem ser reordenados por drag-and-drop; a nova ordem persiste após reload da página
- **AC-2:** Aulas dentro de um módulo podem ser reordenadas por drag-and-drop; a nova ordem persiste após reload
- **AC-3:** O handle de drag é visualmente identificável (ícone ⠿ ou ≡ na esquerda do item)
- **AC-4:** O formulário de módulo (criar/editar) possui campo "Descrição" que salva e recarrega corretamente
- **AC-5:** A description do módulo é exibida abaixo do título no Curriculum Builder quando preenchida
- **AC-6:** Aulas do tipo `text` exibem o TipTap editor em vez do textarea; formatação básica funciona
- **AC-7:** Conteúdo salvo como HTML em `content_body` é renderizado corretamente no player existente (verificar `src/components/` do player)
- **AC-8:** Os botões ↑↓ foram removidos de módulos e aulas
- **AC-9:** Lint, typecheck e build passam sem erros

## Technical Notes

### Instalação das dependências

Verificar `package.json` antes de instalar:

```bash
# dnd-kit (drag-and-drop)
npm install @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities

# TipTap (rich text)
npm install @tiptap/react @tiptap/pm @tiptap/starter-kit
```

### Padrão @dnd-kit/sortable para lista de módulos

```tsx
import { DndContext, closestCenter } from '@dnd-kit/core'
import {
  SortableContext,
  verticalListSortingStrategy,
  useSortable,
  arrayMove,
} from '@dnd-kit/sortable'
import { CSS } from '@dnd-kit/utilities'

// Wrapper do item arrastável
function SortableModule({ module }: { module: ModuleRow }) {
  const { attributes, listeners, setNodeRef, transform, transition } = useSortable({
    id: module.id,
  })
  const style = { transform: CSS.Transform.toString(transform), transition }
  return (
    <div ref={setNodeRef} style={style}>
      <button {...attributes} {...listeners}>
        ⠿
      </button>
      {/* conteúdo do módulo */}
    </div>
  )
}

// Handler de drop — atualiza order no banco
async function handleDragEnd(event: DragEndEvent, modules: ModuleRow[]) {
  const { active, over } = event
  if (!over || active.id === over.id) return
  const oldIndex = modules.findIndex((m) => m.id === active.id)
  const newIndex = modules.findIndex((m) => m.id === over.id)
  const reordered = arrayMove(modules, oldIndex, newIndex)
  // PATCH cada módulo com seu novo order index
  await Promise.all(
    reordered.map((m, i) =>
      fetch(`/api/admin/modules/${m.id}`, {
        method: 'PATCH',
        body: JSON.stringify({ order: i + 1 }),
      })
    )
  )
}
```

### Configuração básica do TipTap

```tsx
import { useEditor, EditorContent } from '@tiptap/react'
import StarterKit from '@tiptap/starter-kit'

function RichTextEditor({ value, onChange }: { value: string; onChange: (html: string) => void }) {
  const editor = useEditor({
    extensions: [StarterKit],
    content: value,
    onUpdate: ({ editor }) => onChange(editor.getHTML()),
  })
  return (
    <div className="border rounded-md">
      {/* Toolbar: Bold, Italic, H2, H3, UL, OL */}
      <EditorContent editor={editor} className="prose max-w-none p-3 min-h-[200px]" />
    </div>
  )
}
```

### API de PATCH de módulos (order)

O endpoint existente `PATCH /api/admin/modules/[id]` já aceita `order` — verificar se aceita, senão adicionar. O PATCH de description é novo campo adicionado na migration 22.1.

### Verificação de regressão no player

O campo `content_body` já era salvo e renderizado. Verificar em `src/components/` o componente de player de aula de texto para confirmar que já faz `dangerouslySetInnerHTML={{ __html: lesson.content_body }}` ou equivalente — o HTML do TipTap é compatível.

## Tasks / Subtasks

- [ ] **Task 1 — Instalar dependências** (AC-1, AC-6)
  - Verificar se `@dnd-kit/core`, `@dnd-kit/sortable`, `@tiptap/react`, `@tiptap/starter-kit` já estão instalados
  - Instalar os que faltam via npm

- [ ] **Task 2 — Drag-and-drop de módulos** (AC-1, AC-3, AC-8)
  - Refatorar `ModuleEditor.tsx` com `DndContext` + `SortableContext`
  - Criar `SortableModuleItem` com handle visual ⠿
  - Handler `handleDragEnd` com PATCH de order
  - Remover botões ↑↓ de módulos

- [ ] **Task 3 — Drag-and-drop de aulas** (AC-2, AC-3, AC-8)
  - Refatorar `LessonEditor.tsx` com `SortableContext` intra-módulo
  - Criar `SortableLessonItem` com handle visual
  - Handler `handleDragEnd` com PATCH de order das aulas
  - Remover botões ↑↓ de aulas

- [ ] **Task 4 — Campo description no módulo** (AC-4, AC-5)
  - Adicionar campo "Descrição" no formulário inline de módulo (criar/editar)
  - Incluir `description` no PATCH de atualização de módulo
  - Exibir description abaixo do título quando preenchida

- [ ] **Task 5 — Rich text editor para aulas de texto** (AC-6, AC-7)
  - Criar componente `RichTextEditor.tsx` usando TipTap + StarterKit
  - Toolbar básica: Bold, Italic, H2, H3, UL, OL
  - Substituir `<textarea>` de `content_body` em `LessonEditor.tsx` pelo componente
  - Verificar que HTML salvo renderiza corretamente no player

- [ ] **Task 6 — QA e build** (AC-9)
  - `npm run lint` sem erros
  - `npm run typecheck` sem erros
  - `npm run build` sem erros
  - Testar: criar módulo com descrição, reordenar módulos, reordenar aulas, criar aula texto com formatação

## File List

| File                                      | Action | Notes                                                   |
| ----------------------------------------- | ------ | ------------------------------------------------------- |
| `src/components/admin/ModuleEditor.tsx`   | Update | DnD para módulos + campo description                    |
| `src/components/admin/LessonEditor.tsx`   | Update | DnD para aulas + substituir textarea por RichTextEditor |
| `src/components/admin/RichTextEditor.tsx` | Create | TipTap wrapper com toolbar básica                       |
| `src/app/api/admin/modules/[id]/route.ts` | Update | Aceitar campo `description` no PATCH                    |
| `package.json`                            | Update | Adicionar @dnd-kit/_ e @tiptap/_ se não existirem       |

## CodeRabbit Integration

> **Story Type:** Frontend
> **Primary Type:** Frontend
> **Complexity:** High (DnD + rich text, dois componentes centrais modificados)

**Specialized Agent Assignment:**

- Primary: @dev, @ux-design-expert

**Quality Gate Tasks:**

- [ ] Pre-Commit (@dev): Run `coderabbit --prompt-only -t uncommitted`
- [ ] Pre-PR (@devops): Run `coderabbit --prompt-only --base main`

**Focus Areas:**

- Regressão em toda a funcionalidade de módulos e aulas (botões de quiz, tarefa, preview, delete)
- Memory leak: TipTap editor deve ser destruído no unmount (`editor.destroy()`)
- PATCH de order: race condition se múltiplos drags rápidos (debounce ou lock)
- DangerouslySetInnerHTML no player: HTML do TipTap deve ser sanitizado se exibido para alunos

**Self-Healing:**

- Mode: light | Max Iterations: 2 | Severity: CRITICAL

## Change Log

| Date       | Agent       | Action                                                                 |
| ---------- | ----------- | ---------------------------------------------------------------------- |
| 2026-06-19 | @sm (River) | Story 22.3 criada — Curriculum Builder DnD + Rich Text — Status: Draft |
| 2026-06-19 | @po (Pax)   | Validação 10/10 — GO — Status: Draft → Ready                           |
